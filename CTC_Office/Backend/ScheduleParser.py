from Graph import Graph, Node #!to export the tracks
from Train import Train, TrainSchedule
import pandas as pd #!to read in the track data
import os #!To check the file stuff
import math

def readSchedule(fn: str = "") -> list:
    return None
